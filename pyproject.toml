[project]
name = "simpe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai",
    "questionary>=2.1.1",
]
[project.scripts]
simpe = "main:main"
analyze = "analyze_results:main"

[tool.uv]
package = true

[tool.setuptools]
py-modules = ["main", "analyze_results"]